import { useRouter } from "expo-router";
import { useMemo, useState } from "react";
import { ActivityIndicator, Alert, FlatList, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { AppHeader } from "@/components/app-header";
import { AppIcon } from "@/components/app-icon";
import { AppScreen } from "@/components/app-screen";
import { AudiencePicker } from "@/components/audience-picker";
import { EmptyState } from "@/components/empty-state";
import { JoyBloom } from "@/components/joy-bloom";
import { MomentCard } from "@/components/moment-card";
import { MoodChip } from "@/components/mood-chip";
import { TagSuggestions } from "@/components/tag-suggestions";
import { useAppTheme } from "@/hooks/use-app-theme";
import { isWithinLastDays, uniqueDayCount } from "@/lib/date";
import type { Mood } from "@/lib/domain";
import { useHappy } from "@/lib/happy-store";

const moods: Mood[] = ["sunny", "peaceful", "proud", "connected"];

export default function TodayScreen() {
  const router = useRouter(); const { colors } = useAppTheme(); const { state, addMoment, refresh } = useHappy();
  const [draft, setDraft] = useState(""); const [mood, setMood] = useState<Mood>("sunny"); const [podId, setPodId] = useState<string | undefined>(); const [tags, setTags] = useState<string[]>([]); const [celebrating, setCelebrating] = useState(false); const [saving, setSaving] = useState(false);
  const moments = useMemo(() => [...state.moments].sort((a, b) => b.createdAt.localeCompare(a.createdAt)), [state.moments]);
  const weekMoments = useMemo(() => state.moments.filter((moment) => isWithinLastDays(moment.createdAt, 7)), [state.moments]);
  const weekDays = uniqueDayCount(weekMoments.map((moment) => moment.createdAt));
  const currentMember = state.members.find((member) => member.id === state.currentMemberId);
  async function saveQuickMoment() { if (!draft.trim() || saving) return; setSaving(true); try { await addMoment({ text: draft, mood, podId, tags }); setDraft(""); setMood("sunny"); setPodId(undefined); setTags([]); setCelebrating(true); } catch { Alert.alert("Moment not saved", "HappyMe couldn’t reach the cloud. Your words are still here so you can try again."); } finally { setSaving(false); } }

  const header = <View style={styles.headerContent}>
    <AppHeader eyebrow="A softer kind of social" title="Good to see you." subtitle="Notice one small thing worth keeping." actionIcon="notifications-outline" actionLabel="Open encouragements" onAction={() => router.push("/kindness" as never)} />
    {state.syncError ? <Pressable accessibilityRole="button" accessibilityLabel="Retry cloud synchronization" onPress={() => void refresh()} style={[styles.syncBanner, { backgroundColor: colors.surfaceAlt }]}><AppIcon name="cloud-offline-outline" size={18} color={colors.coral} /><Text style={[styles.syncText, { color: colors.ink }]}>Showing your last saved view. Tap to retry.</Text><AppIcon name="refresh" size={17} color={colors.muted} /></Pressable> : state.syncing ? <View style={[styles.syncBanner, { backgroundColor: colors.surfaceAlt }]}><ActivityIndicator size="small" color={colors.coral} /><Text style={[styles.syncText, { color: colors.muted }]}>Syncing your private space…</Text></View> : null}
    <View style={[styles.pulseCard, { backgroundColor: colors.ink, shadowColor: colors.shadow }]}><View style={styles.pulseGlowOne} /><View style={styles.pulseGlowTwo} /><View style={styles.pulseTop}><View style={styles.pulseIcon}><AppIcon name="sparkles" size={20} color={colors.ink} /></View><Text style={styles.pulseLabel}>YOUR JOY PULSE</Text></View><View style={styles.pulseStats}><View><Text style={styles.pulseNumber}>{weekMoments.length}</Text><Text style={styles.pulseCaption}>moments this week</Text></View><View style={styles.pulseDivider} /><View><Text style={styles.pulseNumber}>{weekDays}</Text><Text style={styles.pulseCaption}>days noticed</Text></View></View></View>
    <View style={[styles.composer, { backgroundColor: colors.surface, borderColor: colors.border, shadowColor: colors.shadow }]}>
      <View style={styles.composerTitleRow}><View style={[styles.avatar, { backgroundColor: currentMember?.color ?? colors.primary }]}><Text style={styles.avatarText}>{currentMember?.initials ?? "YO"}</Text></View><View style={styles.composerTitleCopy}><Text style={[styles.composerTitle, { color: colors.ink }]}>What made you smile today?</Text><Text style={[styles.composerSubtitle, { color: colors.muted }]}>A sentence is enough.</Text></View><Pressable accessibilityRole="button" accessibilityLabel="Open full moment composer" onPress={() => router.push("/compose" as never)} style={({ pressed }) => [styles.expandButton, { backgroundColor: colors.surfaceAlt, opacity: pressed ? 0.6 : 1 }]}><AppIcon name="expand-outline" size={18} color={colors.muted} /></Pressable></View>
      <TextInput accessibilityLabel="Happy moment" placeholder="The warm sun on my face, a ridiculous joke…" placeholderTextColor={colors.muted} value={draft} onChangeText={setDraft} multiline maxLength={420} style={[styles.input, { color: colors.ink, backgroundColor: colors.background, borderColor: colors.border }]} />
      <TagSuggestions text={draft} mood={mood} selectedTags={tags} onChange={setTags} compact />
      <FlatList horizontal data={moods} keyExtractor={(item) => item} showsHorizontalScrollIndicator={false} contentContainerStyle={styles.moodRow} renderItem={({ item }) => <MoodChip mood={item} selected={mood === item} onPress={() => setMood(item)} />} />
      <View><Text style={[styles.audienceLabel, { color: colors.muted }]}>SHARE WITH</Text><AudiencePicker pods={state.pods} currentMemberId={state.currentMemberId} selectedPodId={podId} onChange={setPodId} compact onCreatePod={() => router.push({ pathname: "/pods", params: { create: "1", request: Date.now().toString() } } as never)} /></View>
      <Pressable accessibilityRole="button" accessibilityLabel="Save happy moment" accessibilityState={{ disabled: !draft.trim() || saving }} disabled={!draft.trim() || saving} onPress={saveQuickMoment} style={({ pressed }) => [styles.saveButton, { backgroundColor: draft.trim() && !saving ? colors.primary : colors.border, opacity: pressed ? 0.75 : 1, transform: [{ scale: pressed ? 0.97 : 1 }] }]}>{saving ? <ActivityIndicator color={colors.ink} /> : <><Text style={[styles.saveButtonText, { color: colors.ink }]}>Keep this moment</Text><AppIcon name="sparkles" size={18} color={colors.ink} /></>}</Pressable>
    </View>
    <View style={styles.sectionHeader}><View><Text style={[styles.sectionTitle, { color: colors.ink }]}>Recent light</Text><Text style={[styles.sectionSubtitle, { color: colors.muted }]}>Yours and your private pods</Text></View><Pressable onPress={() => router.push("/garden" as never)} style={({ pressed }) => ({ opacity: pressed ? 0.55 : 1 })}><Text style={[styles.seeAll, { color: colors.coral }]}>See garden</Text></Pressable></View>
  </View>;

  return <><AppScreen><FlatList data={moments} keyExtractor={(item) => item.id} contentContainerStyle={styles.list} ListHeaderComponent={header} ListEmptyComponent={<EmptyState icon="sunny-outline" title="Your first bright spot lives here" body="Capture one small moment and begin your joy garden." />} ItemSeparatorComponent={() => <View style={styles.separator} />} renderItem={({ item }) => <MomentCard moment={item} authorName={state.members.find((member) => member.id === item.authorId)?.name ?? "Someone"} podName={state.pods.find((pod) => pod.id === item.podId)?.name} />} showsVerticalScrollIndicator={false} /></AppScreen><JoyBloom visible={celebrating} onComplete={() => setCelebrating(false)} /></>;
}

const styles = StyleSheet.create({
  list: { paddingHorizontal: 18, paddingBottom: 32 }, headerContent: { gap: 22, paddingTop: 12, paddingBottom: 16 }, syncBanner: { minHeight: 46, borderRadius: 17, paddingHorizontal: 13, flexDirection: "row", alignItems: "center", gap: 9 }, syncText: { flex: 1, fontSize: 15, lineHeight: 20, fontWeight: "700" }, pulseCard: { minHeight: 168, borderRadius: 28, padding: 22, overflow: "hidden", shadowOpacity: 0.16, shadowRadius: 18, shadowOffset: { width: 0, height: 8 }, elevation: 4 }, pulseGlowOne: { position: "absolute", width: 150, height: 150, borderRadius: 75, backgroundColor: "rgba(246,184,74,0.24)", top: -72, right: -18 }, pulseGlowTwo: { position: "absolute", width: 100, height: 100, borderRadius: 50, backgroundColor: "rgba(158,139,216,0.22)", bottom: -52, left: 96 }, pulseTop: { flexDirection: "row", alignItems: "center", gap: 9 }, pulseIcon: { width: 36, height: 36, borderRadius: 18, backgroundColor: "#F6B84A", alignItems: "center", justifyContent: "center" }, pulseLabel: { color: "#FFF7EF", fontSize: 13, lineHeight: 22, letterSpacing: 1.4, fontWeight: "800" }, pulseStats: { flexDirection: "row", alignItems: "flex-end", marginTop: 22, gap: 24 }, pulseNumber: { color: "#FFFFFF", fontSize: 34, lineHeight: 45, fontWeight: "800" }, pulseCaption: { color: "rgba(255,255,255,0.72)", fontSize: 15, lineHeight: 20, marginTop: 1 }, pulseDivider: { width: 1, height: 46, backgroundColor: "rgba(255,255,255,0.18)", marginBottom: 2 }, composer: { borderRadius: 26, borderWidth: 1, padding: 17, shadowOpacity: 0.07, shadowRadius: 16, shadowOffset: { width: 0, height: 7 }, elevation: 2 }, composerTitleRow: { flexDirection: "row", alignItems: "center", gap: 11 }, avatar: { width: 42, height: 42, borderRadius: 21, alignItems: "center", justifyContent: "center" }, avatarText: { color: "#FFFFFF", fontSize: 15, lineHeight: 20, fontWeight: "900" }, composerTitleCopy: { flex: 1 }, composerTitle: { fontSize: 16, lineHeight: 21, fontWeight: "800" }, composerSubtitle: { fontSize: 15, lineHeight: 20, marginTop: 1 }, expandButton: { width: 38, height: 38, borderRadius: 19, alignItems: "center", justifyContent: "center" }, input: { minHeight: 100, borderRadius: 18, borderWidth: 1, marginTop: 15, paddingHorizontal: 14, paddingVertical: 13, textAlignVertical: "top", fontSize: 16, lineHeight: 23 }, moodRow: { gap: 8, paddingVertical: 14 }, audienceLabel: { fontSize: 13, lineHeight: 18, letterSpacing: 1.05, fontWeight: "800", marginBottom: 7 }, saveButton: { minHeight: 50, paddingHorizontal: 16, borderRadius: 25, marginTop: 14, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 8 }, saveButtonText: { fontSize: 15, lineHeight: 22, fontWeight: "900" }, sectionHeader: { marginTop: 4, flexDirection: "row", justifyContent: "space-between", alignItems: "flex-end" }, sectionTitle: { fontSize: 22, lineHeight: 28, fontWeight: "800", letterSpacing: -0.4 }, sectionSubtitle: { fontSize: 15, lineHeight: 21, marginTop: 2 }, seeAll: { fontSize: 15, lineHeight: 21, fontWeight: "800", minHeight: 44, paddingVertical: 10, justifyContent: "center" }, separator: { height: 13 },
});
